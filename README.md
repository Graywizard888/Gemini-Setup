# Gemini-Setup
A bash script that installs, uninstalls and configure Gemini CLI based on user preference while keeping privacy in mind for termux

##🚀 **Installation guide**

clone repo
```bash
git clone https://github.com/Graywizard888/Gemini-Setup && bash gemini_setup.sh
```
